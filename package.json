{
  "name": "chusxxin-ai-first",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "vue": "^3.5.32"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.36.0",
    "@vitejs/plugin-vue": "^6.0.5",
    "vite": "^8.0.4",
    "wrangler": "^4.88.0"
  }
}
